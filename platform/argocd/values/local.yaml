argo-cd:
  configs:
    params:
      server.insecure: true

  server:
    httproute:
      enabled: true
      parentRefs:
        - group: gateway.networking.k8s.io
          kind: Gateway
          name: envoy-gateway
          namespace: envoy-gateway-system
          sectionName: http
      hostnames:
        - argocd.local
